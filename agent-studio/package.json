{
  "name": "agent-studio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Visual builder for multi-agent Claude workflows with knowledge sources and MCP servers",
  "workspaces": [
    "shared",
    "server",
    "web"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:server dev:web",
    "dev:server": "npm run dev --workspace @studio/server",
    "dev:web": "npm run dev --workspace @studio/web",
    "build": "npm run build --workspace @studio/web",
    "start": "npm run start --workspace @studio/server",
    "seed": "npm run seed --workspace @studio/server",
    "typecheck": "npm run typecheck --workspace @studio/shared && npm run typecheck --workspace @studio/server && npm run typecheck --workspace @studio/web"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=20.11"
  }
}
